"use client";
import React, { FC } from "react";

import Filter from "./Filter";

const MobileFilter: FC<{ open: boolean; closeDrawer: () => void }> = ({
  closeDrawer,
  open,
}) => {
  return <React.Fragment></React.Fragment>;
};

export default MobileFilter;
